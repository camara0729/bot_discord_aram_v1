# Configurações do Bot Discord ARAM
# ================================

# Discord Bot Token (obrigatório)
DISCORD_TOKEN=seu_token_aqui

# Riot API Key (obrigatório)
RIOT_API_KEY=sua_chave_riot_aqui

# URL externa do Render (para keep-alive)
# Ex: https://seu-bot-nome.onrender.com
RENDER_EXTERNAL_URL=

# Frequência de backup automático em horas (padrão: 6)
BACKUP_FREQUENCY_HOURS=6

# Webhook utilizado para backups JSON (obrigatório)
BACKUP_WEBHOOK_URL=

# Webhook de observabilidade (incidentes) - opcional mas recomendado
OPS_WEBHOOK_URL=

# Indicador de ambiente Render (automaticamente definido pelo Render)
RENDER=

# Porta do servidor web (automaticamente definida pelo Render)
PORT=10000

# Caminho do banco (Render utiliza /app/data/bot_database.db)
DATABASE_PATH=bot_database.db

# Política de fair play (limite de incidentes e minutos de bloqueio)
FAIRPLAY_LIMIT=3
FAIRPLAY_TIMEOUT_MINUTES=30
